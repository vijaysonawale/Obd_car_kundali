name: car_kundali
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.3+3

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_blue_plus: ^1.32.11
  permission_handler: ^11.3.1
  path_provider: ^2.1.2
  share_plus: ^7.2.2
  fl_chart: ^0.66.0
  google_mobile_ads: ^5.1.0   # ← AdMob
  in_app_review: ^2.0.9          # Shows native Google Play review sheet
  shared_preferences: ^2.3.3     # Persists "already rated" / cooldown state

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true